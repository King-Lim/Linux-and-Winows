1.计算机模型（指针IP及其指向内容）
  工作原理：取址执行（程序载入至内存，CPU才可取址执行）
  *x86PC刚上电的时候，CPU处于实模式（与保护模式对应，实模式寻址CS：IP（CS左移四位+IP），与保护模式不同）；
  *开机时，CS=0xFFFF；IP=0x0000；
  *寻址0xFFF0（ROM BIOS（Basic Input Output System）映射区）；
  *检查RAM，键盘，显示器，软硬磁盘；
  *将磁盘0磁道0扇区（操作系统的引导扇区）读入内存0x7c00处；
  *设置CS=0x07c0，IP=0x0000；
  
2.启动过程
  加载bootsect.s程序-->加载setup.s程序-->进入保护模式-->进入system模块 

3.多进程执行，核心是进程的切换；
